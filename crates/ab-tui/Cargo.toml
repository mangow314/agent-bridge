# 佔位 crate：依計畫 M5 後、fzf/tmux popup 原型驗證有價值才動工
[package]
name = "ab-tui"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ab-core = { path = "../ab-core" }
