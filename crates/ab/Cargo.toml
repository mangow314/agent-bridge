[package]
name = "ab"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ab-core = { path = "../ab-core" }
# 只為 main 起手的 signal(SIGPIPE, SIG_DFL) 一行（架構 §5）；零傳遞依賴
libc = "0.2"
